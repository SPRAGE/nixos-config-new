# Trading Binary Download Configuration
# This file contains URLs, checksums, and version information for trading binaries

# Version information
BINARY_VERSION="0.2.0"
BINARY_DATE="2025-01-09"

# Download URLs
# Replace these with your actual download URLs
PRIMARY_DOWNLOAD_URL="https://github.com/your-org/trading-binaries/releases/download/v${BINARY_VERSION}/trading-x86_64-linux.tar.gz"
MIRROR_DOWNLOAD_URL="https://releases.example.com/trading/v${BINARY_VERSION}/trading-x86_64-linux.tar.gz"

# Expected file information
EXPECTED_FILENAME="trading-x86_64-linux.tar.gz"
EXPECTED_SIZE_MB="50"  # Approximate size in MB

# SHA256 checksums
# Generate with: sha256sum trading-x86_64-linux.tar.gz
# Replace with actual checksums when you have the real binaries
EXPECTED_SHA256="PUT_ACTUAL_SHA256_HERE"

# Alternative checksums for verification
# You can add multiple checksums for different versions
declare -A VERSION_CHECKSUMS=(
    ["1.0.0"]="PUT_SHA256_FOR_VERSION_1_0_0_HERE"
    ["0.9.0"]="PUT_SHA256_FOR_VERSION_0_9_0_HERE"
)

# Binaries that should be present in the archive
EXPECTED_BINARIES=(
    "analysis-server"
    "auth-server"
    "financial-data-consumer"
    "historical-data-updater"
    "index-frontend"
    "ingestion-server"
    "test-clickhouse"
    "ws_manager"
    "ws-subscriber"
)

# Retry configuration
MAX_RETRIES=3
RETRY_DELAY=5

# Timeout for downloads (in seconds)
DOWNLOAD_TIMEOUT=300
